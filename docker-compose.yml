services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "sqlite+aiosqlite:///./data/cash_controller.db"
      LOG_FORMAT: json
    env_file:
      - .env
    volumes:
      - app_data:/app/data

volumes:
  app_data:
